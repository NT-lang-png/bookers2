<div>
  <h2>Books</h2>

     <table>

       <tr>
         <th></th>
         <th>Title</th>
         <th>Opinion</th>
       </tr>

        <% @book.each do |book| %>

       <tr>
         <td><%= image_tag book.user.get_image(100,100) %></td>
         <td><%= book.title %></td>
         <td><%= book.body %></td>
       </tr>
     </table>
        <% end %>
</div>