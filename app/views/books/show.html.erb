<h2>Book detail</h2>

<table>
  <tr>
    <td><%#= image_tag @book.user.get_image(100,100) %><%= @book.user.name %></td>
    <td><%= @book.title %></td>
    <td><%= @book.body %></td>
    <td><%= link_to "Edit", edit_book_path(@book) %></td>
    <td>Destroy</td>
  </tr>
</table>
